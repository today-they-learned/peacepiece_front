import React from "react";
import ModalFrame from "../Modal/ModalFrame";

function Main() {
  return (
    <div>
      <ModalFrame>
        <h3 style={{ margin: "0.6rem" }}>정말 로그아웃 하시겠어요 ?</h3>
      </ModalFrame>
    </div>
  );
}

export default Main;
